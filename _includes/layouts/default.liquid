<!DOCTYPE html>
<html lang="{{ page.lang | default: site.lang | default: "en" }}">
  {%- include head.html -%}
  <body class="bg-black font-white">
    <div class="container max-w-4xl mx-auto px-3">
        {{ content }}

    </div>
    {%- include footer.html -%}
    
  </body>
</html>
